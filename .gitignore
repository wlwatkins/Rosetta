# Build output and packaged zips
.output
.rosetta-src.tar
stats.html
stats-*.json

# WXT generated types and config (recreated by `wxt prepare` on install)
.wxt

# Dependencies
node_modules

# Logs and debug dumps
*.log
npm-debug.log*
about-gpu-*.txt

# Serena's per-machine project config
.serena

# Editor / OS noise
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db

# Local environment overrides
.env
.env.*
!.env.example
web-ext.config.ts

# ORT wasm binaries, copied in by scripts/copy-ort-wasm.mjs
public/wasm/
.opus-export/


# Converted model, hosted rather than committed (365 MB, and GitHub
# rejects files over 100 MB). Rebuild with scripts/convert-opus-int8.py.
model/

# Upstream weights downloaded for the conversion
opus/
